[package]
name = "ocamlrs_test_stubs"
version = "0.1.0"
edition = "2018"

[lib]
crate-type = ["staticlib", "cdylib"]

[dependencies]
ocaml = { path = "..", features = ["bigarray-ext"] }

ndarray = "^0.14.0"
